#include<stdio.h>
#include<math.h>

int main(){
    int a,b;
    a = 4, b=3;
    printf ( "The value of  a +b is %d\n", a+b);
     printf ( "The value of  a - b is %d\n", a-b); // thats why use \n for gap
      printf ( "The value of  a*b is %d", a*b);
       printf ( "The value of  a/b is %d\n", a/b);

    //    printf("the valu of 4 to the power 5 is %f", pow(4,5)); //wont run if math.h is not header
     printf("the valu of 4 to the power 5 is %f", pow(4,5));
    return 0;
    
    return 0;
}